name: publish

on: [push]

jobs:
  publish-sshd-image:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Build the sshd Docker image
      run: |
        echo $CR_PAT | docker login ghcr.io -u stevegroom --password-stdin
      env:
        CR_PAT: ${{ secrets.CR_PAT }}